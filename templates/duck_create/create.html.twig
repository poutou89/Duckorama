{% extends 'base.html.twig' %}

{% block title %}Hello DuckCreateController!{% endblock %}

{% block main %}
    <main>
        <div class="container">
            <div class="form">
                {{form_start(DuckForm)}}
                    {{form_widget(DuckForm)}}
                    <button>Ajouter</button>
                {{form_end(DuckForm)}}
            </div>
        </div>
    </main>
{% endblock %}
